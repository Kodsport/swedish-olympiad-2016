INT(1, 100000, n)
SPACE
INT(1, 1000000, h)
NEWLINE
SET(xbound = 0)
REPI(i, n)
	INT(xbound, 1000000, x)
	SET(xbound = x + 1)
	SPACE
	INT(0, h-1, y)
	IF(i == 0 || i == n-1)
		ASSERT(y == 0)
	END
	SPACE
	INT(0, 1)
	NEWLINE
END
EOF
